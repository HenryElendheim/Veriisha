import 'package:flutter/material.dart';
import 'app.dart';

// The one entry point. Everything else is under lib/, and the game rules live in
// the separate pure-Dart engine package this app depends on.
void main() => runApp(const VeriishaApp());
